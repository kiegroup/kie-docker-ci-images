<component>

  <files>

    <!-- The current project site report. -->
    <file>
      <source>${project.build.directory}/kie-wb-site.jar</source>
      <outputDirectory>./site</outputDirectory>
      <destName>kie-wb-site.jar</destName>
    </file>
    <file>
      <source>${kie.docker.kie-wb-base.path}/assembly/site-web.xml</source>
      <outputDirectory>./site/WEB-INF</outputDirectory>
      <destName>web.xml</destName>
    </file>

  </files>
  
</component>